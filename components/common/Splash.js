import React, { Component } from 'react';
import { View } from 'react-native';

//still have to work on some sort of animation that will appear when first loading the app

const Splash = () => {
  return (
    <View style={styles.splashStyle}>
      
    </View>
  );
};

const styles = {
  splashStyle: {
    backgroundColor: '#ff69b4',
    flex: 1
  }
};

export { Splash };
